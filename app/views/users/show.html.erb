<h2>Hello <%= @user.email %></h2>
<hr class="black-line">
<ul class="list-unstyled">
  <% @datehash.each do |date, array_links| %>
    <ul class="date list-unstyled"> <%= date %>
    <% array_links.each do |clicked_link| %>
    <li> <%= link_to(clicked_link.link.url, clicked_link.link.url) %> </li>
    <% end %>
  <% end %>

</ul>


  